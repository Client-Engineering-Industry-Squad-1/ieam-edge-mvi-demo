apiVersion: v1
kind: Service
metadata:
  name: {{ include "mvi.fullname" . }}
  labels:
    {{- include "mvi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mvi.selectorLabels" . | nindent 4 }}
