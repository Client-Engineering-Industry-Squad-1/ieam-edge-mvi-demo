apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mvi.fullname" . }}-test-connection"
  labels:
    {{- include "mvi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mvi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
